# Application
PORT=4536                           # Port to run the backend server
NODE_ENV=development                # development | production

# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=parssub

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT (authentication)
JWT_SECRET=change_this_secret       # REQUIRED: change in production
JWT_EXPIRES_IN=7d                   # e.g. 7d, 24h

# URLs / CORS
BASE_URL=http://localhost:4536      # Used in scripts and tests
BACKEND_URL=http://localhost:4536  # Backend public URL (CORS, API refs)
FRONTEND_URL=http://localhost:8903  # Frontend URL (used in notifications / web-app links)

# SMS Service
SMS_API_KEY=                         # Your SMS provider API key
SMS_OTP_TEMPLATE_ID=736133           # template id used to send OTP codes (production)
SMS_WALLET_CHARGE_TEMPLATE_ID=      # template id for wallet charge (optional)
SMS_ORDER_CONFIRMATION_TEMPLATE_ID=
SMS_ORDER_DELIVERED_TEMPLATE_ID=

# Zibal (online payment)
ZIBAL_MERCHANT=zibal
ZIBAL_FACILITY_API_KEY=

# TRON (cryptocurrency)
TRON_NETWORK=shasta                 # 'mainnet' or 'shasta'
TRON_MASTER_WALLET=                 # Master wallet address to collect user payments
TRONGRID_API_KEY=

# Saman Bank (card-to-card monitoring)
SAMAN_USERNAME=
SAMAN_PASSWORD=

# Telegram bot (notifications)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHANNEL_ID=

# Optional / Legacy
# BASE_URL and BACKEND_URL can be used interchangeably by some scripts

# Notes:
# - Do NOT commit secrets (JWT_SECRET, SMS_API_KEY, SAMAN_*, TRONGRID_API_KEY, TELEGRAM_BOT_TOKEN) to public repos.
# - Fill only the values you need for your environment. Leave others blank if unused.
# - After editing, run the server and verify services that require credentials (Telegram, Saman, Tron, SMS) are working.
