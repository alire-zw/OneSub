import type { Metadata } from "next";
import { generatePageMetadata } from "@/utils/metadata";

export const metadata: Metadata = generatePageMetadata({
  title: "داشبورد",
  description: "داشبورد کاربری وان‌ساب - مدیریت سفارشات و اشتراک‌ها",
});
