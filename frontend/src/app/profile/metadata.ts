import type { Metadata } from "next";
import { generatePageMetadata } from "@/utils/metadata";

export const metadata: Metadata = generatePageMetadata({
  title: "پروفایل",
  description: "پروفایل کاربری وان‌ساب - مدیریت اطلاعات شخصی و اشتراک‌ها",
});
